<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>BoneSpy | Upload X-ray</title>
  <style>
    body {
      background-color: #111;
      color: #fff;
      font-family: 'Segoe UI', sans-serif;
      margin: 0;
      padding: 0;
    }

    header {
      background-color: #000;
      padding: 1rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      border-bottom: 2px solid #444;
    }

    .logo {
      font-size: 1.8rem;
      font-weight: bold;
      text-align: center;
      flex: 1;
    }

    .menu-button {
      background: none;
      border: none;
      color: white;
      font-size: 1.5rem;
      cursor: pointer;
      padding-right: 1rem;
    }

    .container {
      padding: 2rem;
    }

    input[type="file"] {
      background-color: #222;
      color: #fff;
      border: none;
      padding: 0.5rem;
    }

    input[type="submit"] {
      margin-left: 10px;
      padding: 0.5rem 1rem;
      background-color: #03a9f4;
      border: none;
      color: white;
      cursor: pointer;
      border-radius: 5px;
    }

    .preview {
      margin-top: 2rem;
      padding: 1rem;
      background-color: #222;
      border: 1px solid #444;
      border-radius: 10px;
      max-width: 600px;
    }

    img.preview-image {
      max-width: 100%;
      max-height: 400px;
      border-radius: 8px;
      display: block;
      margin-top: 1rem;
    }
  </style>
</head>
<body>

  <header>
    <div class="logo">BoneSpy</div>
    <button class="menu-button">&#9776;</button>
  </header>

  <div class="container">
    <h2>Upload an X-ray</h2>

    <form action="/upload" method="post" enctype="multipart/form-data">
      <input type="file" name="file" accept="image/*">
      <input type="submit" value="Upload">
    </form>

    <% if @message %>
      <p><%= @message %></p>
    <% end %>

    <% if @filename %>
      <div class="preview">
        <h3>Preview:</h3>
        <img class="preview-image" src="/uploads/<%= @filename %>" alt="Uploaded X-ray">
      </div>
    <% end %>
  </div>

</body>
</html>